The two main forms of modern RAM are static RAM (SRAM) and dynamic RAM (DRAM),Both static and dynamic RAM are considered volatile because their state is lost or reset when power is removed,Most modern operating systems employ a method of extending RAM capacity known as "virtual memory"